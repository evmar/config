#!/bin/bash
#
# This script runs google-chrome with the same arguments,
# but filters out console spam.

EXTRA_FLAGS=--disable-hang-monitor
export AUTO_PROXY=$_AUTO_PROXY

exec /usr/bin/google-chrome $EXTRA_FLAGS "$@" >& /dev/null

